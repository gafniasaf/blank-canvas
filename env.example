DATABASE_URL=postgres://localhost:5432/ai_books
OPENAI_API_KEY=your-openai-key
MCP_URL=http://127.0.0.1:3001

# Where INDD files live (absolute or relative to project root)
INDESIGN_INPUT_DIR=designs

# Optional: override embedding model/dimension if you change providers
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIM=1536

# Optional: batch sizes and limits
EMBED_BATCH_SIZE=50
MAX_ROWS_PER_RUN=0           # 0 = no limit

# Optional: override output paths
EXTRACT_OUTPUT_JSON=output/extracted.json

